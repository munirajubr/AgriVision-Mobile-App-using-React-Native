// Correct: Added default export
import { Text, View } from "react-native";

const NPKUploadPage = () => {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>NPK Upload Screen</Text>
    </View>
  );
};

export default NPKUploadPage;
